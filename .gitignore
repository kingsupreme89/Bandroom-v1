# Build output
bin/
obj/
dist/
*.dll
*.exe
*.pdb

# IDE
.vs/
.vscode/
*.user
*.sln.iml

# OS
.DS_Store
Thumbs.db

# App data
WebView2Data/

# Secrets -- never committed. See GoogleAuthService.cs's ClientSecret property for what goes here.
google_client_secret.local.txt
Crash*.txt

# Admin marketplace override token -- never committed AND deliberately never packaged into the
# build (see WebBridge.cs's AdminTokenPath comment for why this one is different from the
# Google secret above). Only ever present on the app owner's own dev machine.
admin_token.local.txt

# Release archives
*.zip
squirrel_releases/
